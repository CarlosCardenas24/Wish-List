generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:dev.sqlite"
}

model Session {
  id          String    @id
  shop        String
  state       String
  isOnline    Boolean   @default(false)
  scope       String?
  expires     DateTime?
  accessToken String
  userId      BigInt?
}

model Product {
  id          String   
  name        String
  shopId      String
  variantId   String   @id
  variantName String
  price       Float
  image       String
  quantity    Int
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}


model User {
  userId    Int @id
  wishList  WishList[]
}

model WishList {
  id          Int @id
  quantity    Int
  variantName String
  user        User  @relation(fields: [id], references: [userId])
}

//entry
//{
//  userId: "user123",
//  wishlist: "[ {quantity: 1, variantId: 12323} , (quantity: 5, variantId: 1231123), (quantity: 5, variantId: 123098) ]" // variantId
//}